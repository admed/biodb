# from django.apps import AppConfig
# from watson import search as watson

# class ProjectsConfig(AppConfig):
#     name = "projects"
#     def ready(self):
#         RObject = self.get_model("RObject")
#         watson.register(RObject, fields=("creator__username",))
